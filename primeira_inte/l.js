let currossel =0
const slides=documento.querysectorall('imgens_carrossel')

const slide=documento.querysectorall(input[type='radio'])


function showslide(index){
slides.forEach((sildes , id ) =>{
slides.style.opacity = i === idex?


(slide,i)
slide[i].checked = i === index


})




function ne(){
    currossel = (currenstlide ++)% slides.length;
showslaide(currentSlide);
}

setInterval( nestslide , 3000;) 

radio.forEach(radio,i) =>{
 radio.addventlister('click' , () =>{
  currentslide = i ;
showslide( currentSlide)
})
}

